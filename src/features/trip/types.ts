export enum THRESHOLD {
  MAX_ROW = 5,
}

export type StatusField = 'Not Started';

export type Inputs = {
  startDate: string;
  endDate: string;
};

export type ValidationInputField = {
  required: boolean;
};
